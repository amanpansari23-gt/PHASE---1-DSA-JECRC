Question = code of sum of digits for geeksforgeeks


class Solution {
    void solve(int n,int &sum)
    {
        //Base case
        if(n==0)
        {
            return;
        }
        
        //Recursive case
        int rem = n%10;//Last digit
        sum = sum + rem;
        solve(n/10,sum);
    }
  public:
    int sumOfDigits(int n) {
        // code here
        int sum = 0;
        solve(n,sum);
        return sum;
    }
};


Question = code for factorial using recursion gfg


class Solution {
    int fact(int n)
    {
        //base case
        if(n==0)
        return 1;
        
        //Recursive case 
        return n*fact(n-1);
    }
  public:
    int factorial(int n) {
        // code here
        return fact(n);
    }
};

Question = code for binary search using recursion



#include <iostream>
using namespace std;
//Binary search using Recursion
bool binarySearch(int arr[],int target, int s,int e)
{
  //base case
  if(s>e)
  {
    return false;
  }
  
  int mid = (s+e)/2;
  if(arr[mid] == target)
  {
    return true;
  }
  else if(arr[mid]>target)
  {
    return binarySearch(arr,target,s,mid-1);
  }
  else
  {
    return binarySearch(arr,target,mid+1,e);
  }
}
int main() 
{
   int arr[6] = {1,2,3,4,5,6};
   int n=6;
   int target = 5;
   if(binarySearch(arr,target,0,n-1) == true)
   {
     cout<<"present";
   }
   else
   {
     cout<<"Absent";
   }
   
   return 0 ;
}

Question = code for reverse array using recursion 


#include <iostream>
using namespace std;

void reverse(int arr[],int i,int j)
{
  //base case
  if(i>j)
  {
    return;
  }
  ///recursive case
  swap(arr[i],arr[j]);
  
  reverse(arr,i+1,j-1);
}
int main() 
{
  int arr[6] = {1,2,3,4,5,6};
  int n= 6;
  reverse(arr,0,n-1);
  
  for(int i = 0; i < n;i++)
  {
    cout<<arr[i]<<" ";
  }
  return 0;
}

Question = code of table of 2 using recursion

#include <iostream>
using namespace std;

void table(int i, int n)
{
    if(i == n+1)
    {
        return;
    }
    cout<<2*i<<endl;
    table(i+1,n);
}

int main() 
{
    int n;
    int i = 1;
    cin>>n;
    table(i,n);

    return 0;
}

Queastion = min and max of array of gfg

class Solution {
  public:
  
  void solve(vector<int>&arr, int i, int min, int max, vector<int>&ans)
  {
      //base case
      if(i == arr.size())
      {
          ans.push_back(min);
          ans.push_back(max);
          return;
      }
      
      if(arr[i]<min)
      min = arr[i];
      
      if(arr[i]>max)
      max = arr[i];
      
      solve(arr,i+1,min,max,ans);
  }
    vector<int> getMinMax(vector<int> &arr) {
        vector<int> ans;
        int min = INT_MAX;
        int max = INT_MIN;
        
        solve(arr,0,min,max,ans);
        return ans;
    }
};

Question = code for check if array is sorted of gfg

class Solution {
  public:
  bool solve(vector<int> &arr, int i)
  {
      //base case
      if(i == arr.size()-1)
      {
          return true;
      }
      
      if(arr[i]>arr[i+1])
      return false;
      
      return solve(arr,i+1);
  }
    bool isSorted(vector<int>& arr) {
        return solve(arr,0);
        
    }
};

Question = code for leetcode problem 231

class Solution {
public:
bool solve(int i, int n)
{
    //base case
    if(pow(2,i) == n)
    return true;

    if(pow(2,i) > n)
    return false;

    return solve(i+1,n);
    
}
    bool isPowerOfTwo(int n) {
         return solve(0,n);
    }
};

Question = code for reverse digit of gfg

class Solution {
  public:
  int reverse (int n, int rev)
  {
      //base case
      if(n == 0)
      return rev;
      
      int rem = n%10;
      rev = rev*10+rem;
      
      //recursive case
      return reverse(n/10,rev);
  }
    int reverseDigits(int n) {
        int rev = 0;
        return reverse(n,rev);
        
    }
};

Question = code for non linear recursion function

#include <iostream>
using namespace std;
void print(string str, int i, int n, string ans)
{
    //base case
    if(i == n)
    return;

    //ignore
    print(str,i+1,n,ans);

    //include
    ans += str[i];
    cout << ans << " ";
    print(str,i+1,n,ans);
}

int main() 
{
   string str;
   cin >> str;
   string ans="";
   print(str,0,str.length(),ans);

   return 0;
}
