Question = code of problem 94 leetcode



/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
    void solve(TreeNode *root,vector<int>&arr)
    {
        //base case
        if(root == NULL)
        {
            return;
        }

        solve(root->left,arr);
        arr.push_back(root->val);
        solve(root->right,arr);
    }
public:
    vector<int> inorderTraversal(TreeNode* root) {
        vector<int>arr;

        solve(root,arr);
        return arr;
    }
};


Queston = code of problem number 144 leetcode

/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
    void solve(TreeNode *root,vector<int>&arr)
    {
        if(root == NULL)
        {
            return;
        }
        arr.push_back(root->val);
        solve(root->left,arr);
        solve(root->right,arr);
    }
public:
    vector<int> preorderTraversal(TreeNode* root) {
        vector<int>arr;

        solve(root,arr);
        return arr;
    }
};


Question = code of problem number 145 leetcode


/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
     void solve(TreeNode *root,vector<int>&arr)
    {
        if(root == NULL)
        {
            return;
        }
        solve(root->left,arr);
        solve(root->right,arr);
        arr.push_back(root->val);
    }
public:
    vector<int> postorderTraversal(TreeNode* root) {
        vector<int>arr;

        solve(root,arr);
         return arr;
        
    }
};

Question = code of problem number 257



/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
    void solve(TreeNode *root, string path, vector<string>&ans)
    {
        //base case
        if(root == NULL)
        {
            return;
        }
        path += to_string(root->val);
        if(root->left == NULL && root ->right == NULL)
        {
            ans.push_back(path);
        }
        else
        {
            solve(root->left,path + "->",ans);
            solve(root->right,path + "->",ans);
        }
    }
public:
    vector<string> binaryTreePaths(TreeNode* root) {
        vector<string>ans;
        solve(root,"",ans);
        return ans;

    }
};

Question = problem number 102 leetcode

class Solution {
public:
    vector<vector<int>> levelOrder(TreeNode* root) {
        vector<vector<int>>ans;

        if(root == NULL)
        return ans;

        queue<TreeNode*> q;
        q.push(root);

        while(!q.empty())
        {
            int n = q.size();//uss particular level me kitne node h
            vector<int>v;
            while(n>0)
            {
                TreeNode *temp = q.front();
                q.pop();
                v.push_back(temp->val);

                if(temp -> left != NULL)
                q.push(temp -> left);

                if(temp -> right != NULL)
                q.push(temp -> right);

                n--;
            }
            ans.push_back(v);
        }
        return ans;
    }
};

Question = code for left view p=of binary tree of gfg

/*
class Node {
public:
    int data;
    Node* left;
    Node* right;

    Node(int val) {
        data = val;
        left = nullptr;
        right = nullptr;
    }
};
*/

class Solution {
  public:
    vector<int> leftView(Node *root) {
        vector<int> ans;
        
        if(root == NULL) 
        return ans;
        
        queue<Node*>q;
        q.push(root);
        
        while(!q.empty())
        {
            int n = q.size();//uss particular level me kitne node h
            ans.push_back(q.front()->data);
            
            while(n>0)
            {
                Node *temp = q.front();
                q.pop();
                

                if(temp -> left != NULL)
                q.push(temp -> left);

                if(temp -> right != NULL)
                q.push(temp -> right);

                n--;
            }
        }
        return ans;
    }
};

Question = problem 199 leetcode


class Solution {
public:
    vector<int> rightSideView(TreeNode* root) {
        vector<int>ans;

        if(root == NULL)
        return ans;

        queue<TreeNode*> q;
        q.push(root);

        while(!q.empty())
        {
            int n = q.size();//uss particular level me kitne node h
            ans.push_back(q.front()->val);
            
            while(n>0)
            {
                TreeNode *temp = q.front();
                q.pop();
                

                if(temp -> right != NULL)
                q.push(temp -> right);

                if(temp -> left != NULL)
                q.push(temp -> left);

                n--;
            }
        }
        return ans;            
    }
};

Question = size of binary tree of gfg


/*
Definition for Node
struct Node {
    int data;
    struct Node* left;
    struct Node* right;

    Node(int val) {
        data = val;
        left = right = NULL;
    }
};
*/
class Solution {
     void preorder(Node* node,int &count)
    {
        if(node == NULL)
        return;

        preorder(node->left,count);
        preorder(node->right,count);
        
        count++;
    
    }
  public:
    int getSize(Node* root) {
        int count = 0;
        preorder(root,count);
        return count;
    }
};

Question = sum of binary tree of gfg


/*
Definition for Node
class Node {
public:
    int data;
    Node* left;
    Node* right;

    Node(int val) {
        data = val;
        left = right = nullptr;
    }
};
*/
class Solution {
    void preorder(Node* node,int &sum)
    {
        if(node == NULL)
        return;
        
        sum += node->data;
        preorder(node->left,sum);
        preorder(node->right,sum);
    
    }
  public:
    int sumBT(Node* root) {
        int sum =  0;
        preorder(root,sum);
        return sum;
    }
};

Question = count leaves in binary tree of gfg

/* A binary tree node has data, pointer to left child
   and a pointer to right child
struct Node
{
    int data;
    Node* left;
    Node* right;
}; */

// Class Solution
class Solution {
    public:
    void preorder(Node* node, int &count)
    {
        if(node == NULL)
        return;
        
        if(node->left == NULL && node->right == NULL)//calculate leaf node
        {
            count++;
            return;
        }
        
        preorder(node->left,count);
        preorder(node->right,count);
    }
  
    // Function to count the number of leaf nodes in a binary tree.
    int countLeaves(Node* root) {
        int count = 0;
        preorder(root,count);
        return count;
    }
};

Question = count non leaf nodes in tree

/*Structure of the node of the tree is as
struct Node
{
    int data;
    struct Node* left;
    struct Node* right;
};
*/
// you are required to complete this function
// the function should return the count of Non-Leaf Nodes
class Solution {
  public:
  void preorder(Node* node, int &count)
    {
        if(node == NULL)
        return;
        
        if(node->left == NULL && node->right == NULL)//calculate leaf node
        {
            
            return;
        }
        count++;
        
        preorder(node->left,count);
        preorder(node->right,count);
    }
  
    int countNonLeafNodes(Node* root) {
        int count = 0;
        preorder(root,count);
        return count;
    }
};

Question = height of binary tree of gfg

/*
Definition for Node
class Node {
public:
    int data;
    Node* left;
    Node* right;

    Node(int val) {
        data = val;
        left = right = nullptr;
    }
};
*/

class Solution {
  public:
    int height(Node* root) {
        if(root == NULL)
        return -1;
        
        return 1+max(height(root->left),height(root->right));
    }
};

Question = problem 104 leetcode

class Solution {
public:
    int maxDepth(TreeNode* root) {
        if(root == NULL)
        return 0;

        return 1+max(maxDepth(root->left),maxDepth(root->right));
    }
};

Question = problem number 103 leetcode


class Solution {
public:
    vector<vector<int>> zigzagLevelOrder(TreeNode* root) {
        vector<vector<int>>ans;

        if(root == NULL)
        return ans;

        queue<TreeNode*> q;
        q.push(root);
        bool flag = true;//direction 
        //true : left to right
        //false : right to left

        while(!q.empty())
        {
            int n = q.size();//uss particular level me kitne node h
            vector<int>v(n);
            int idx;//iterate to 1-D array

            for(int i = 0;i<n;i++)//us current level me jitne nide h sabme iterate karenge
            {
                TreeNode *temp = q.front();
                q.pop();
                if(flag == true)
                {
                    idx = i;
                }
                else
                {
                    idx = n-1-i;
                }
                v[idx] = temp->val;

                if(temp -> left != NULL)
                q.push(temp -> left);

                if(temp -> right != NULL)
                q.push(temp -> right);
            }
            flag = !flag;//change the direction
            ans.push_back(v);
        }
        return ans;
    }
};

Question = larrgest value in each level of gfg

class Solution {
  public:
    vector<int> largestValues(Node* root) {
        vector<int> ans;
        
        if(root == NULL)
        return ans;
        
        queue<Node*>q;
        q.push(root);
        
        while(!q.empty())
        {
            int n = q.size();
            int max = INT_MIN;
            while(n>0)
            {
                Node*temp = q.front();
                q.pop();
                if(temp->data > max)
                max = temp->data;
                
                if((temp->left) != NULL)
                q.push(temp->left);
                
                if((temp->right) != NULL)
                q.push(temp->right);
                
                n--;
            }
            ans.push_back(max);
        }
        return ans;
    }
};
